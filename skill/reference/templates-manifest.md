# Templates manifest — what each file does

The `templates/` directory of this skill contains the reference Bio Study Tutor source. Copy these into the new project as described.

## Copy verbatim (no edits needed)

| File | Goes to | Purpose |
|---|---|---|
| `extract_pptx.py` | `tools/extract_pptx.py` | Walks `.pptx` slides, dumps to `raw/extracted/<slug>.md` with `## Slide N` anchors |
| `extract_docx.py` | `tools/extract_docx.py` | Walks `.docx` paragraphs, preserves headings, table extraction |
| `extract/__init__.py` | `tools/extract/__init__.py` | Empty package marker |
| `extract/frontmatter.py` | `tools/extract/frontmatter.py` | YAML frontmatter parser/writer for wiki pages |
| `extract/sections.py` | `tools/extract/sections.py` | Markdown body → ordered-dict of H2/H3 sections; `section_aliases()` canonicalizes section heading names |
| `extract/wikilinks.py` | `tools/extract/wikilinks.py` | `[[wikilink]]` regex + slug→id resolver |
| `extract/tables.py` | `tools/extract/tables.py` | Markdown table parser |
| `validate.py` | `tools/validate.py` | JSON-Schema validation + edge endpoint integrity check |
| `render_index.py` | `tools/render_index.py` | Regenerates `wiki/index.md` from KG |
| `__init__.py` | `app/__init__.py` | Package shim that exposes `app.app` (required for shinyapps.io entrypoint) |
| `kg_loader.py` | `app/kg_loader.py` | Loads wiki + KG into memory at app startup |
| `llm.py` | `app/llm.py` | OpenRouter client (model: `google/gemma-4-26b-a4b-it`) |
| `games.py` | `app/games.py` | LLM-driven quiz/flashcard/match generators with structured-JSON output |
| `wikilink.py` | `app/wikilink.py` | Renders `[[wikilink]]` syntax to clickable HTML anchors |
| `prebuild.py` | `app/prebuild.py` | One-shot script to LLM-generate starter content (use sparingly — hand-author starter content for first launch quality) |
| `.python-version` | `.python-version` | Pins Python 3.11 (required for shinyapps.io) |
| `.rscignore` | `.rscignore` | Exclude `.env`, source binaries from rsconnect bundle |
| `.gitignore` | `.gitignore` | Standard ignores + `.env` excluded |
| `.env.example` | `.env.example` | Template `OPENROUTER_API_KEY=...` placeholder |

## Copy with subject-specific edits

| File | Goes to | What to edit |
|---|---|---|
| `build_kg.py` | `tools/build_kg.py` | `NODE_TYPES` set, `EDGE_RELATIONS` set, `SECTION_TO_RELATION` mapping |
| `app.py` | `app/app.py` | `CHAPTER_TITLES`, `TYPE_LABELS`, navbar title, theme palette if desired, hard-coded subject mentions |
| `graph_viz.py` | `app/graph_viz.py` | `TYPE_COLORS` dict to include any new node types |
| `build_print_version.py` | `tools/build_print_version.py` | `TYPE_ORDER`, `CHAPTER_LABELS` |
| `study-guide-compact.template.html` | `print/study-guide-compact.html` | Hand-authored 4-page condensed reference (this is the heaviest customization step) |

## Required environment

After scaffolding, install dependencies:
```bash
pip3 install shiny pyvis requests pyyaml markdown networkx python-pptx python-docx jsonschema
```

## What's NOT in templates/ (must be authored fresh)

- `wiki/` — every page is subject-specific, must be hand-authored
- `app/starter_content/{quizzes,flashcards,match}.json` — hand-authored for first-launch quality
- `kg/schema/*.schema.json` — generated from chosen schema, but the per-type schema files can be auto-generated from the base schema
- `print/study-guide.html` — generated by `tools/build_print_version.py`, but the styling references are in the script
- `screenshots/` — captured by user from the running app
